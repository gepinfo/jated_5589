spring:
  data:
    mongodb:
      uri: ${MONGO_DB_URL}
server:
  port: 8000

logging:
  file:
    name: logs/jated.log
    path: jated
  level:
    com.geppetto: info
  pattern:
    dateformat: "dd-MM-yyyy hh:mm:ss aa"



























